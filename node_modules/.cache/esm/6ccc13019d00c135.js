let match,evaluate;_9d0‍.x([["default",()=>_9d0‍.o]]);_9d0‍.w("minimatch",[["default",["match"],function(v){match=v}]]);_9d0‍.w("../utils/eval",[["default",["evaluate"],function(v){evaluate=v}]]);/**
 * 过滤文件
 * @Author: Junting.liu
 * @Date: 2019-09-25 16:13:36
 * @Last Modified by: Junting
 * @Last Modified time: 2022-10-26 11:26:15
 */




/**
 * 根据用户回答的问题，来决定删除哪些文件
 * @param {Object} files
 * @param {Array} filters
 * @param {Object} Data
 * @param {Function} done
*/
function filter (files, filters, data, done) {
  if (!filters) {
    return done();
  }
  const fileNames = Object.keys(files);
  Object.keys(filters).forEach(glob => {
    fileNames.forEach(file => {
      if (match(file, glob, { dot: true })) {
        const condition = filters[glob];
        if (!evaluate(condition, data)) {
          delete files[file];
        }
      }
    });
  });
  done();
};

_9d0‍.d(filter);